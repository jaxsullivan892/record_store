// Deprecated: root Dockerfile replaced by per-service Dockerfiles in ./frontend and ./backend
// Keep this file as a placeholder. Build and run services via docker-compose which uses
// the individual Dockerfiles at ./frontend/Dockerfile and ./backend/Dockerfile.